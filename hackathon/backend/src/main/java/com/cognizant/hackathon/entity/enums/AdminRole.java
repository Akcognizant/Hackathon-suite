package com.cognizant.hackathon.entity.enums;

public enum AdminRole {
    ADMIN,
    JUDGE,
    /** Self-registered hackathon participant (browses events, forms teams, submits projects). */
    PARTICIPANT
}
